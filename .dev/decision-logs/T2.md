Missing data.csv at execution time — No file existed when T2 started. I used a temporary 3-row stub to verify endpoints, then ran the full smoke test once your real data appeared. That was a workflow workaround, not a kill-criteria failure.

No unit tests — T2’s contract explicitly waives them; smoke test is the gate. The executor skill prefers tests always, but the subtask packet overrides that here.

Adversarial gap deferred — No automated test for malformed date strings in the CSV. Logged in CHANGELOG.MD; eval harness (T6) is the intended gate.

Docker healthcheck uses curl — docker-compose.yml healthchecks with curl, but python:3.11-slim may not include it. Out of scope for T2 (T1 owns the Dockerfile/compose). Worth watching if the db service never goes healthy.

[I copied and pasted this manually - Ale (user)]